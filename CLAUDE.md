# LEANOVATE

Internal isometric room planner for an interior design team serving condo investors in Thailand. Designers use this tool to place real purchasable furniture into isometric room layouts, generate cost summaries, and present proposals to clients.

## Reference Docs (read before making changes)
- @docs/product-spec.md — Features, user stories, MVP scope, roles
- @docs/tech-stack.md — Technology choices, folder structure, AI pipeline, cost estimates
- @docs/schema.md — Database tables, fields, relationships, data flows
- @docs/design.md — Visual system (colors, typography, spacing, component styling)
- @docs/state-map.md — Zustand store domains and ownership rules
- @docs/integration-contracts.md — External API contracts (Supabase, Replicate, screenshot extraction, Three.js)
- @docs/implementation-plan.md — Build phases, dependencies, what to test
- @docs/designer-workflow.md — External photo preprocessing (Nano Banana) before upload (Phase 7+)
- @docs/phase-history.md — Completion notes per shipped phase (read when touching a subsystem the phase introduced)

## Tech Stack
- Frontend: React 18+ / TypeScript 5+ / Vite 5+ / Three.js / Zustand 4+ / shadcn/ui / Tailwind 3+
- Backend: Supabase (auth, PostgreSQL, storage, edge functions)
- AI Pipeline: TRELLIS via Replicate (multi-image → .glb 3D model, does its own background removal). .glb files are rendered directly in the Three.js canvas (no sprite generation). Flat items (rugs, etc.) bypass TRELLIS entirely and render as textured floor planes.
- Hosting: Vercel (frontend) + Supabase Cloud (backend)
- Icons: Lucide React (included with shadcn/ui)

## Current Phase
V1 COMPLETE (Phases 1–6) + Phase 7 COMPLETE + Phase 8 COMPLETE. All MVP functionality shipped against the new 3D canvas.

## Follow-up Improvements (backlog — not yet scoped into a phase)
Captured during Phase 8 review. Prioritize before first paying customer touches the product.

- ~~**Lighting effect** + **Ceiling light**~~ ✅ Shipped 2026-04-27 (Phase A centroid fixture → Phase B placed furniture, same day). **Ceiling lights are placed furniture now**, not a per-room finish. Designers add/move/delete downlights from the catalog like any other item.
  - **Schema:** `furniture_categories.emits_light boolean` + `placed_furniture.light_settings jsonb` (`{ enabled, preset, temperature_k, intensity }`); `mount_type` enum gained `'ceiling'`. Migrations `20260427020000_add_ceiling_mount_type.sql` (enum ADD VALUE in its own file — PG can't use a new enum value in the same tx that added it) and `20260427020100_lights_as_placed_furniture.sql` (columns + seed + cleanup). The Phase A `RoomFinishes.lighting` JSON + `finish_materials` lighting rows were dropped (no paying users; clean-slate per CLAUDE.md).
  - **Seed:** "Ceiling Light" category (`mount_type='ceiling'`, `emits_light=true`, `is_flat=false`, `default_block_size='small'`) with two default items (Recessed Downlight + Pendant Sphere) + one variant each. Both are `render_status='completed'`, `render_approval_status='approved'` on insert — procedural meshes skip TRELLIS entirely. `isItemArchitectural` extended to include `mount_type='ceiling'` and any `emits_light=true` category so `createVariant`/`retryRender` keep skipping the pipeline.
  - **Rendering:** `src/lib/roomScene.ts` now exports `buildLightFixture(kind, mountType, settings)` — returns a procedural luminaire group (cord+globe for pendant, flush ring+lens for recessed) + a SpotLight (ceiling mount, points down, 60° cone) or PointLight (other mounts, omnidirectional — wired for future lamps). `applyLightSettings` mutates intensity/color/emissive in place; `kelvinToRgb` (Tanner Helland) drives both. `createFurnitureGroup` has a new short-circuit: if `emitsLight=true`, build the fixture synchronously and return — no flat/.glb branches run.
  - **Placement:** `useCanvasStore.placeItem`/`placeItems` now call `defaultYcmForMount` which sets `y_cm = room.ceiling_height_cm` for `mount_type='ceiling'` items. Drag preserves y_cm. Placement ghost positions at ceiling height in `useCanvasInteractions`. Fixture kind picked from item name (`.includes('pendant')` → pendant, else recessed) — simple for 2 seeded items; extend to a real field if style count grows.
  - **Per-instance UI:** `src/components/editor/RightPanel/PlacedLightingSection.tsx` — preset row (Warm/Neutral/Cool/Off) + temperature + brightness sliders, writes to `placed_furniture.light_settings` via `useCanvasStore.updateLightSettings`. Rendered in `FurnitureProperties` when `category.emits_light=true`. Slider change rebuilds the fixture group (cheap — procedural, no .glb fetch); if drag-tick rebuild ever stutters, swap for in-place `applyLightSettings` via a per-id refs map.
  - **Studio rig** (ambient + sun + fill) stays per-app in `useRoomLighting`, toggleable via `useUIStore.studioLights` (localStorage-persisted, defaults on). Lives in the persistent `lightingGroupRef` outside `shellGroup` so the toggle doesn't trigger a shell rebuild.
- ~~**Surfaces (walls, floors)**~~ ✅ Shipped 2026-04-21 (commit `09c7bfc`). `finish_materials` gained `texture_url` + `tile_size_cm`. 13 CC0 tileable JPGs from Poly Haven committed to `public/textures/{floors,walls}/` + seeded as presets (7 floors, 6 walls). New helpers in `src/lib/roomScene.ts` — `getFinishAppearance`, `getSharedTexture` (URL-keyed cache — one GPU texture shared across all meshes using it), `setWorldSpaceUVs` (bakes physical tile rate into mesh UVs so the shared texture stays at `repeat=(1,1)` regardless of surface size), `applyFinishTexture`. Wired into `buildRoomShell` floor + both wall paths (PlaneGeometry no-cutout, ShapeGeometry with cutouts) and into `renderRoomPreview.ts`. Finishes tab splits swatches into **Colors** vs **Textures** sub-groups per finish type — plain paints (hex) + tileable textures (URL) coexist. Door/window finish rows + `'door' | 'window'` from `FinishType`/`RoomFinishes` dropped entirely (fixtures handle those now); renderers use hardcoded `#C4B8A8` / `#DDEEFF` fallback panel + glass colors. Lighting kept in schema/enum but hidden from the picker pending the ceiling-light feature. Normal/roughness maps still deferred — today's setup is single-albedo, which is the Sims aesthetic (option 2 PBR is a later upgrade).
- ~~**Flat model background removal**~~ ✅ Addressed 2026-04-22 via designer workflow rather than code. Flat render code is unchanged — `fillFlatPlane` / `fillVerticalFlatPlane` in `src/lib/roomScene.ts` still don't enable `transparent: true`/`alphaTest`. The primary guidance in `AddFurnitureModal` Step 2 flat-item banner was flipped to **"crop tight to the item's real edges"** (JPG or PNG both work) since rectangular rugs + posters cover ~80% of cases and transparent PNGs from Nano Banana aren't reliable. Transparent-PNG is now the *fallback* rule for round/irregular shapes only (round rugs, fringed rugs, sculptural wall art). i18n strings: `flatBannerRuleCrop` (new, primary) + `flatBannerRuleAlpha` (new, fallback) replace `flatBannerRulePng`. If round/irregular cases start shipping to clients, enable `transparent: true` + `alphaTest: 0.5` on the two fill functions (1-line each) so the fallback PNGs actually render alpha.
- **Doors, windows, curtains** — partial progress 2026-04-21 (commit `54be275`):
  - ✅ **TRELLIS bypass** — doors/windows (`mount_type='wall'` categories) never go through the 3D pipeline. `createVariant` and `retryRender` in the catalog store gate on a new `isItemArchitectural` helper. Do NOT re-wire these through TRELLIS — the model distorts framed/glazed geometry badly. Architectural fixtures always render as room-shell primitives.
  - ✅ **Door/window render path** — room shell always uses a cutout + textured panel (from the variant's uploaded photo) or a generic glass+frame fallback. `.glb` loading for these was removed from `roomScene.ts` and `renderRoomPreview.ts`.
  - ✅ **Curtains** — procedural pleated cloth panels in `roomScene.ts` (`buildCurtain` + `buildCurtainPanel` + shared `getFabricNormalTexture`). Reads `RoomWindow.curtain_style` + `curtain_color`. "Closed" = single panel across the full opening + 12cm overhang; "open" = two gathered side panels. Rod-to-floor length, thin dark rod, shared linen-weave normal map. Rendered in both live canvas and perspective preview.
  - ⚠️ **Still backlog** — proper door *leaf* meshes (frames, handles, swing direction indicators) and proper window *mullion* meshes (frames, grids of glazing). Today both are single textured planes. The architectural gate is in place; when modeled, they slot into the same render path as curtains.
- ~~**Show ceiling when camera below ceiling**~~ ✅ Shipped 2026-04-21. RoomCanvas now toggles the ceiling mesh each frame in design mode: `ceiling.visible = camera.position.y < ceilingH`. Roam mode still builds the shell with ceiling always visible. Binary toggle for now — add an opacity fade if the pop is jarring in practice.
- ~~**Catalog UI (left sidebar) — Sims-style shopping feel**~~ ✅ Shipped 2026-04-22. CatalogPanel now renders a 2-column grid of square tiles with cached isometric `.glb` snapshots (migration `20260422000000_variant_thumbnails.sql` adds `furniture_variants.thumbnail_path`; renderer in `src/lib/renderVariantThumbnail.ts`; backfilled lazily on catalog mount). Click tile = place on canvas; ⋯ icon opens a detail drawer for per-variant actions, render review, and admin approval. Hover-to-rotate live preview deliberately deferred.
- ~~**Flip furniture (handedness mirror)**~~ ✅ Shipped 2026-04-22. New `placed_furniture.mirrored boolean` column (migration `20260428010000_placed_furniture_mirrored.sql`) — per-instance horizontal mirror for items with left/right SKUs (L-shape sectionals, handed desks). `useFurnitureLayer` applies `group.scale.x = mirrored ? -1 : 1` on initial build and on in-place transform updates. Three.js handles the determinant-sign flip automatically, so face culling + lighting stay correct; caveat is text/logos baked into a product photo read backwards (designers will learn which items don't flip cleanly). UI: Flip button in `FurnitureProperties` → Actions, next to Rotate; active when `mirrored=true`. Keyboard shortcut **F** (guarded against input/textarea focus; disabled in shape-edit mode). Full undo/redo via new `MirrorCommand`. Ghost preview reflects `mirrored=false` at placement (first click sets `mirrored: false`; designer hits F after to flip if needed).
- **Collapsible right-panel sections** — as the right panel accumulates sections (Color, Details, Lighting, Artwork, Placement, Actions, Source link), it may start to feel cluttered. Don't blanket-convert to expand/collapse — that adds a click to common operations (rotate/size) and introduces "I can't find the setting" risk. Instead, when real designer usage surfaces a "I keep scrolling past X" signal, collapse *only* the rarely-touched sections (Placement fine-tune cm inputs, Source link) while leaving always-open the ones touched every proposal (Color, Rotation/Size, Lighting presets). Persist collapsed state to localStorage per-section-name so it's consistent across sessions.
- ~~**Room Preview mode — remove or upgrade**~~ ✅ Replaced 2026-04-22. The separate Preview modal (+ its parallel scene builder, SSAO/bloom pipeline, and GPU path tracer) was deleted in favor of a simple **"Export view"** floating button on the live canvas — `src/lib/exportCanvasView.ts` temporarily bumps the renderer's drawingBuffer to 4K (`setSize(3840, targetH, false)`, preserving CSS display size), renders one frame, `toBlob`s to PNG, auto-downloads, and restores the original size. Filename: `{project}_{room}_{YYYYMMDD_HHmm}.png`. No modal, no wall selector, no round-trip; what the designer sees IS what gets exported. Removed: `RoomPreviewModal.tsx`, `renderRoomPreview.ts` (~800 lines), EditorPage Preview button + state, `three-gpu-pathtracer` + `n8ao` + `postprocessing` deps (~440 KB of JS), `rooms.preview_image_url` column (migration `20260428000000_drop_room_preview_image.sql`), `Room.preview_image_url` TS field + `updateRoom` writer, `roomPreview.*` / `common.preview` / `editor.preview` / `editor.previewRoom` i18n strings (en + th). Button lives bottom-right alongside the grid toggle (bottom-left) and camera mode toggle (top-right); hidden in roam mode since first-person captures are weirdly framed.
- ~~**Image upload → 3D render waiting UX**~~ ✅ Shipped 2026-04-21. AddFurnitureModal now enqueues variants into `useRenderQueueStore` and closes immediately. A global `RenderQueueTray` (mounted in `App.tsx`, bottom-right) shows per-variant stage (`uploading → creating → trellis → ready | failed`), upload progress bar, elapsed time, and a Review CTA that opens `ModelApprovalModal` inline. Ready-stage entries auto-dismiss when the designer approves or rejects (subscribes to catalog store's `render_approval_status`). TRELLIS observation uses Zustand subscribe — zero new polling. `generate-3d-model` now propagates the real Replicate error text on prediction start failure so 500s are self-diagnosing.
- **Furniture styles & templates regenerate/redesign button** — when a furniture layout or design style template is applied, designers need a "Regenerate" / "Redesign" button that swaps the picked products for alternates *within the same constraints*: same category, same style tag(s), same effective block size (so placement and grid snap stay valid). Should randomize per-slot (not a full re-roll of the whole room) so designers can lock the picks they like and reshuffle only the slots they don't. Useful as the core of the "one-click generate with regenerate/shuffle" story from product-spec.md user story #7.
- ~~**Picture frames with custom art**~~ ✅ Shipped 2026-04-27. New "Picture Frame" category (`is_flat=true`, `flat_orientation='vertical'`, `accepts_art=true`, `mount_type='floor'` so designers can author frame items). Frame items track a real product (price + outer dims + `furniture_items.mat_opening_cm` jsonb `{w, h}` for the visible art rectangle). Empty placed frame renders the frame product photo as an upright plane via new `fillVerticalFlatPlane` in `src/lib/roomScene.ts`; with art, a second plane sized to `mat_opening_cm` overlays 2mm in front (`fillFrameArtOverlay`). New `art_library` table (`scope enum('private','team')`, `aspect_ratio float`, RLS: user sees own private + all team art + admin sees all) + `frame-art` public bucket (path `{uploader_id}/{uuid}.ext`). New `useArtStore` (raw-fetch writes per concurrency rule). `placed_furniture.art_id` FK with `ON DELETE SET NULL` — art deletion silently reverts the frame to empty. ArtPickerModal (My / Team / Upload tabs) filters by mat aspect ratio ±10%; scope toggle flips private↔team with no admin approval (per explicit product call). Rendering call sites updated: `useFurnitureLayer` subscribes to art store + includes `artUrl` in rebuild signature; `usePlacementGhosts` passes through the new params. AddFurnitureModal Step 1 shows mat-opening inputs only when `selectedCategory.accepts_art`, with outer-dimension bounds check before `createItem`. **Not yet rendered in room perspective preview or project thumbnails** — those paths still skip flat items entirely (pre-existing gap for rugs too; leave for a separate flat-preview backlog item).
- ~~**Edit Shape mode — click-to-pick, hover-to-move, click-to-release**~~ ✅ Shipped 2026-04-22. Vertex + wall push/pull are now click-arm → cursor-follow → click-release. In `src/components/editor/RoomCanvas/hooks/useCanvasInteractions.ts`, the shape-edit branch of `onPointerDown` checks for an armed `vertexDragRef` / `wallDragRef` first and commits if present; otherwise it arms on a handle/wall hit **without** calling `setPointerCapture` or disabling OrbitControls, so the user can still orbit/pan mid-edit. Follow logic in `onPointerMove` is gated on `e.buttons === 0` (same pattern as the existing fixture pickup flow) so held-button drags fall through to OrbitControls. `onPointerUp` is now a no-op for vertex/wall (the second click commits via `commitShapeEdit`). Escape restores the armed ref's pre-arm geometry snapshot without persisting. Hover cursor shows `grabbing` while armed as a visible cue. Fixture pickup already used this model — it was the reference implementation.
- ~~**Wall-length labels should follow the wall line**~~ ✅ Shipped 2026-04-22. Two issues in `updateDimLabels` (`src/components/editor/RoomCanvas/hooks/useThreeScene.ts`): (1) labels appeared static because the animate-loop closure read `room.id` from the first-mounted room (useEffect deps `[]` → scene persists across room switches, closure stale). Fixed by reading `useProjectStore.getState().selectedRoomId` each frame so the label loop always tracks the currently selected room. (2) Labels are anchored at the wall's top-edge midpoint (ceiling-height midpoint) — "on top of the wall" — and re-project every frame, so they slide with the midpoint as walls are pushed/pulled. Any future room-scoped animate-loop reads should use the same pattern (project-store selectedRoomId, not the closure-captured `room`).
- ~~**Admin page auto-navigates back to Pending Approval while on Catalog tab**~~ ✅ Fixed 2026-04-22. Root cause: `AdminPage` kept `activeTab` in `useState('pending')`. Any incidental remount of the page (e.g., `ProtectedRoute` briefly re-rendering during an auth-store profile refresh, or a Fast Refresh during dev) reset the tab to `'pending'`. Fix: promoted to `useUIStore.adminTab` (session-local, same pattern as `sidebarTab` / `rightPanelTab`), so tab selection survives any remount. Type exported from `useUIStore.ts` as `AdminTab`.
- ~~**Admin page content doesn't load on first navigation — refresh fixes it**~~ ✅ Fixed 2026-04-22 (+ follow-up for the dashboard the same day). Silent hang (nothing in console). Root cause: the supabase JS client deadlock from CLAUDE.md #8. Any on-mount `supabase.from().select()` can silently hang when another client op is still in flight — and that "other op" turns out to be on every navigation path between pages, not just dashboard → admin. Fixes, all converting on-mount reads to `rawSelect` (raw `fetch` against PostgREST):
  - `PendingApprovalQueue.loadPending` (admin Pending tab — 4 queries)
  - `CatalogOverview.load` + `reloadData` (admin Catalog tab)
  - `useProjectStore.loadProjects` (dashboard mount)
  - `useProjectStore.loadProject` (editor mount)
  - `useProjectStore.loadFinishMaterials` (editor mount)

  Raw fetch bypasses the client's lock entirely. LinkHealth/Team/AIUsage already used raw fetch or edge functions. **New rule-of-thumb:** on-mount reads that run alongside navigation should use `rawSelect`, not `supabase.from()`. CLAUDE.md #8's old carve-out ("read-only load* queries may still use the client") assumed nothing else was running, but in practice any navigation from a page that issued a client read can leave the lock held. Precedent: all writes (`rawUpdate`, `rawInsert`) in the catalog store already do this. Remaining client-using `load*` methods in `useCatalogStore` (loadItems, loadCategories, loadStyles, loadItemStyles, loadVariantsForItem) haven't bitten yet but should migrate if/when they cause a hang.
- ~~**Approved items still show as draft in both catalog panel (editor) and Admin → Catalog**~~ ✅ Fixed 2026-04-22 with a belt-and-suspenders approach across three layers:
  - **Enriched optimistic update** in `approveItem` / `rejectItem` (`src/stores/catalog/itemsSlice.ts`) — now also propagates `reviewed_by` + `reviewed_at` into the local `items.map()` so downstream filters/counts based on those fields don't lag.
  - **Force store hydration post-approval** in `PendingApprovalQueue.handleApprove` / `handleReject` — after `approveItem`/`rejectItem` runs, we also call `useCatalogStore.getState().loadItems()`. This covers the case where the admin landed directly on `/admin` without opening the editor first: `useCatalogStore.items` was `[]`, so the optimistic `state.items.map(...)` silently no-op'd; a follow-up `loadItems()` pulls fresh rows from DB. Safe on `/admin` because CatalogPanel isn't mounted (no polling collision).
  - **CatalogOverview subscribes to `useCatalogStore.items`** and merges store changes into its local `items` state. Previously it loaded fresh on mount and never saw in-flight approvals; now an approval in `PendingApprovalQueue` fans out to Admin → Catalog without needing a remount. `setItems` merges by id so other local-only changes (e.g., regenerate-sprites loading flags) don't get clobbered.
- **Admin can edit furniture details in the Admin Catalog tab** — designers currently have no way to fix a wrong scrape, update a price that changed on Shopee/IKEA, or correct dimensions after a variant has been approved. Today the admin Catalog tab (`src/components/admin/CatalogOverview.tsx`) is read-only — it surfaces item + variant rows but can't mutate anything except approval status. Build an inline edit flow:
  - **Item-level fields:** `name`, `description`, `width_cm`, `depth_cm`, `height_cm`, `category_id`, `source_url`, `mat_opening_cm` (frame items), `is_flat_override`, `block_size_override`, style tags (via `furniture_item_styles`).
  - **Variant-level fields:** `color_name`, `price_thb`, `source_url`, per-variant `width_cm`/`depth_cm`/`height_cm` overrides, `original_image_urls[]` (reorder + add + remove — first image is the flat-item asset). Re-running TRELLIS after an image edit should go through `retryRender` so architectural/flat bypasses still apply.
  - **UX:** expand-in-place row ("Edit" button per item → opens a drawer or inline form with all fields), Save/Cancel. Price change should bump `updated_at` so the editor's staleness check fires on the next project open (see `docs/schema.md` §placed_furniture staleness logic).
  - **Writes via raw fetch** per CLAUDE.md rule #9 — `rawUpdate('furniture_items', …)` / `rawUpdate('furniture_variants', …)`, then optimistic `useCatalogStore.setState(...)` so CatalogPanel in the editor reflects the change without a reload. Style-tag edits go through `furniture_item_styles` insert/delete.
  - **Permissions:** admin-only (role check, same pattern as `AdminRoute`). Designers keep today's "edit own drafts before submission" flow — this is specifically about post-approval maintenance.
  - **Not in scope for v1:** bulk edit, image cropping in-app, variant reordering via drag (a sort_order number input is fine).
- ~~**Project selection page canvas thumbnails**~~ ✅ Shipped 2026-04-25. DashboardPage cards now show a 16:10 isometric dollhouse snapshot of the project's primary room (first by `sort_order`). Renderer in `src/lib/renderProjectThumbnail.ts` (640×400 PNG, 45°/35.264° ortho camera, frustum fit to room AABB + 12% pad); migration `20260425000000_project_thumbnails.sql` adds `projects.thumbnail_path`. Auto-refresh fires from EditorPage Save (fire-and-forget after `saveProject` + `savePlacedFurniture` so it doesn't block the toast); manual refresh via hover-reveal button on each card calls `refreshProjectThumbnailFromDb` (loads fresh DB data, safe because CatalogPanel isn't mounted on dashboard). Wall-side picked per winding (CCW → `FrontSide`, CW → `BackSide`) so near walls cull for both default rectangles and shape-edited polygons. `LayoutGrid` icon placeholder when no thumbnail yet; `updated_at` query param busts the cache after refresh.

## Phase Completion Notes
Historical implementation details for shipped phases (5, 6, 7, 8) have moved to `docs/phase-history.md`. Read that file when touching a subsystem a phase introduced.

## Code Style
- TypeScript strict mode. **Verify with `npm run build`, not `tsc --noEmit`** — the project's `tsc -b` respects `verbatimModuleSyntax` (so `import type` is required for type-only imports); `--noEmit` is more permissive and will let broken code through.
- Functional components with hooks.
- One Zustand store per file in src/stores/ (6 stores — see state-map.md). For stores that grow past ~500 lines, compose from slices under `src/stores/<domain>/` — see `src/stores/catalog/` as the reference (slices + `pipeline.ts` + a shared `SliceCreator<T>` alias so cross-slice `get()` type-checks).
- Zustand v5: use `create<T>(fn)` (single-paren). The curried `create<T>()(fn)` form can behave surprisingly when composing slices.
- Supabase client in src/lib/supabase.ts.
- Use Tailwind utility classes + CSS variables from design.md.
- Use shadcn/ui components as the base UI library.
- Use Lucide React for all icons.

## File Structure — Large Components

When a component passes ~600 lines, split it. Reference implementations: `src/components/editor/RightPanel/`, `AddFurnitureModal/`, `RoomCanvas/`.

- **Directory + `index.tsx`**: Vite resolves the directory automatically so call-site imports don't change. Shared CSS goes in `styles.ts`; component-local CSS stays inline in the owning file.
- **Three.js / canvas components** (`RoomCanvas/`): one `useThreeScene` hook owns mount + render loop and returns a `SceneContext` holding every scene ref. Downstream hooks (`useRoomShell`, `useFurnitureLayer`, etc.) **destructure the refs they need into local vars at the top of the hook** — required by `react-hooks/immutability`. Pure helpers (raycasters, grid math, geometry commands) live in `lib/`.
- **Ordered event handlers**: when the priority of checks matters on a single event (e.g. `pointerdown` → shape-edit handle → fixture placement → furniture placement → fixture pickup → furniture raycast → deselect), keep them in ONE dispatcher function. Don't split listeners by concern — you'll silently break the order.
- **Cross-cutting state**: if handlers span multiple "steps" (like `AddFurnitureModal`'s screenshot extract touching both step-1 fields and step-2 variant 0), keep state + handlers in `index.tsx` and make step files purely presentational. Don't introduce context or lift to hooks just to split files.

## Commands
- `npm run dev` — start dev server
- `npm run build` — production build
- `npm run lint` — ESLint + Prettier

## Key Rules
1. **Read the relevant doc before implementing any feature.** Each doc owns a specific domain — check the scope table at the top of each file.
2. **Never duplicate state across Zustand stores.** Catalog Store is the single source of truth for all product data. Other stores reference by ID only. See state-map.md.
3. **Cost summary is always computed live, never stored.** Derive from placed furniture variant prices (Catalog Store) + manual costs (Project Store).
4. **Furniture uses a parent + variants model.** Parent item holds shared details. Each color variant has its own `original_image_urls[]` (1+ photos), price, link, and `.glb`. See schema.md.
5. **Approval is post-TRELLIS, not pre-TRELLIS.** Variants start with `render_approval_status='pending'`. After TRELLIS generates a .glb, designer reviews it in ModelApprovalModal and approves/rejects/retries. Approval is a quality signal — it does NOT gate canvas placement. Flat items (category.is_flat or item.is_flat_override) skip TRELLIS entirely and auto-approve.
6. **Designer uploads images manually.** The scraper only extracts text data (name, description, dimensions). It does NOT download product images.
7. **Three.js is the only renderer.** Live editor canvas (`RoomCanvas/index.tsx`), room perspective preview (`renderRoomPreview.ts`), and the .glb approval modal all run client-side Three.js. PixiJS is gone (Phase 8). Sprite generation is gone — `.glb` files render directly.
8. **All user-facing strings go through `useTranslation()` — English AND Thai.** The app ships bilingual (`src/locales/en.json` + `src/locales/th.json`, wired via `src/lib/i18n.ts`). Any new text in components/modals/toasts/alerts/aria-labels/placeholders/titles/`confirm()` prompts/`showToast` messages must use `t('namespace.key')` with the key added to **both** locale files — keeping them in sync is part of "done", not a follow-up. Hardcoded English in a new component is a regression even if the tests pass. Rule of thumb: if a Thai-speaking designer would see it on screen, it needs a Thai string. Pattern: match the enclosing namespace (e.g. `addFurniture.*`, `editor.properties.*`, `artPicker.*`); a `defaultValue` fallback on `t()` is acceptable belt-and-braces but does NOT excuse skipping the locale entries. Verify with: `python3 -c "import json; a=set(json.load(open('src/locales/en.json'))['<ns>']); b=set(json.load(open('src/locales/th.json'))['<ns>']); print(a ^ b)"` — output should be `set()`.
9. **NEVER use `supabase.from()` or `supabase.storage` anywhere. Reads too, not just writes.** The Supabase JS client deadlocks when two async operations run concurrently through the same instance — no error, no timeout, just a permanent silent hang. Page navigation (dashboard → editor → admin) and in-parallel fan-outs (e.g. EditorPage calls `loadVariantsForItem` in a for-loop over every placed item, concurrently with `loadItems` + `loadCategories` + `loadStyles` from CatalogPanel's mount) guarantee concurrent client calls. The old carve-out ("load* may use the client because it's the only thing running on initial mount") was wrong — on any real usage pattern, many load*s race at mount. **Use raw fetch helpers from `@/lib/supabase`:** `rawSelect` (reads), `rawInsert`, `rawInsertMany`, `rawUpdate`, `rawUpdateWhere`, `rawDelete`, `rawDeleteWhere`, `rawStorageUpload`, `rawStorageDownload`, `getPublicStorageUrl`, plus `getAuthToken` and `invokeEdgeFunction` (in `useCatalogStore.ts`). Additionally, action callbacks must NOT trigger re-fetches via `load*` while CatalogPanel's 5s polling is active — trust the local Zustand state updates instead. The only safe direct Supabase client uses left are: **`supabase.auth.*`** calls in `useAuthStore.ts` (login/signup/signout — these don't run concurrently with polling). Every on-mount read across editor, dashboard, admin, and `renderProjectThumbnail.ts` uses `rawSelect` today — keep it that way.

## API Keys Required
- **Supabase:** Project URL + anon key + service role key (from Supabase dashboard)
- **Replicate:** API token (from replicate.com — for TRELLIS model)

## Database
- All tables defined in docs/schema.md
- Row-Level Security (RLS) on all tables
- Designers see own drafts + approved items; admins see everything
